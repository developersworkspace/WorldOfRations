export class SupplementFeedstuff {
    
    id: string;
    text: string;
    weight: number;

    constructor(id: string, text: string, weight: number) {
        this.id = id;
        this.text = text;
        this.weight = weight;
    }
}