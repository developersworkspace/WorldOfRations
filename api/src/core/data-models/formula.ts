// Imports models
import { Element } from './element';

export class Formula {

    id: string;
    name: string;

    constructor(id: string, name: string) {
        this.id = id;
        this.name = name;
    }
}